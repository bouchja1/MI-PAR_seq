/* 
 * File:   main.cpp
 * Author: honza
 *
 * Created on 7. říjen 2012, 15:41
 */

#include <iostream>

#include "BinaryVector.h"
#include "Worker.h"
#include "TaskParser.h"
#include <math.h>
#include "ParentNode.h"
#include "Node.h"
#include "LeafNode.h"

using namespace std;





void testParser(){
    TaskParser* parser=new TaskParser(true);
    TaskCrate* taskCrate=parser->getTaskFromFile("testTasks/test33.txt") ;
    
    delete parser;
    delete taskCrate;    
}

/*
 * 
 */
int main(int argc, char** argv) {

    // testVector();
    // testCountNeigbhours();
    // testNextIterationRules();
    // testIteratForQTimes(3);
    // testDistance();    
    // testWeight();
    // testGenerator();

    //testWorker();

   // testWorker2();
    //testWorker3();
    
    testParser();     
   
    
     Node* n = new ParentNode(3,3,true);
    n->printNode();
    
    
    Node* c1 = n->makeLeftTrueNode(3);
    c1->printNode();
    Node* c2 = n->makeRightFalseNode(3);
    c2->printNode();
    
    Node* d1 = c1->makeLeftTrueNode(3);
    d1->printNode();
    Node* d2 = c1->makeRightFalseNode(3);
    d2->printNode();
    
    delete c1;
    delete c2;
    delete d1;
    delete d2;
    delete n;  
    

    return 0;
}
